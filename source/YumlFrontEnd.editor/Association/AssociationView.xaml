<UserControl x:Class="YumlFrontEnd.editor.AssociationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:YumlFrontEnd.editor"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance local:AssociationViewModel}">
  <StackPanel Orientation="Vertical">
    <DockPanel>
      <local:EditableTextBlock 
        Watermark="Enter name here"
        HorizontalAlignment="Stretch"
        FontWeight="Bold"
        Margin="0,5"
        cal:Bind.Model="{Binding}"/>
      <local:ButtonBar 
        HorizontalAlignment="Right"
        HasNewButton="False"
        HasColorPicker="True"
        cal:Bind.Model="{Binding}" />
    </DockPanel>
    <DockPanel
      HorizontalAlignment="Stretch">
      <ComboBox
        DockPanel.Dock="Left"
        x:Name="Associations"
        Margin="0,5"/>
      <ComboBox
        DockPanel.Dock="Left"
        x:Name="Classifiers"
        Margin="5"/>
    </DockPanel>
  </StackPanel>
</UserControl>
