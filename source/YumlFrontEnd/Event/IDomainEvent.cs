namespace Yuml
{
   /// <summary>
   /// This is just a simple marker interface. 
   /// All domain events must implement this interface 
   /// to get registered at the Message system.
   /// </summary>
   public interface IDomainEvent { }
}